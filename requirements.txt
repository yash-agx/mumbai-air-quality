# Runtime dependencies for the deployed Streamlit app (app.py).
#
# Direct dependencies only, pinned; pip resolves the rest. The pipeline needs
# more than this -- scipy, matplotlib and scikit-learn are used to build the
# data under data/processed, not to serve it -- so those live in
# requirements-dev.txt and are deliberately not installed in the cloud.

streamlit==1.62.0
pydeck==0.9.3
# Charts for the time-patterns section, per the project's stack spec.
plotly==7.0.0
pandas==3.0.5
numpy==2.5.2
pyarrow==25.0.1
requests==2.34.2
python-dotenv==1.2.3
# Browser geolocation for the "Use my location" button. 12 KB wheel,
# streamlit is its only dependency, static HTML component (no JS build).
streamlit-js-eval==1.0.0
