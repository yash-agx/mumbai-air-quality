# Everything requirements.txt has, plus what the offline pipeline needs.
#   data/fetch.py           OpenAQ pull            python-dateutil
#   scripts/02_features.py  OSM features           scipy, matplotlib
#   model/interpolate.py    cross-validation       scipy, scikit-learn
#
# None of these are imported by app.py at runtime: the extrapolation masks the
# app needs are baked to data/processed/grid_masks.npz by
# `python model/interpolate.py --masks`.

-r requirements.txt

scipy==1.18.1
matplotlib==3.11.1
scikit-learn==1.9.0
python-dateutil==2.9.0.post0
